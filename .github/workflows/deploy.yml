name: Deploy to Production

on:
  push:
    branches:
      - deploy

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          ref: deploy

      - name: Deploy to server
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          script: |
            set -e
            
            PROJECT_DIR="/home/sulbaranjc/apps/clientes_frontend_react"
            
            echo "════════════════════════════════════════"
            echo "📥 ACTUALIZANDO CÓDIGO DESDE GITHUB"
            echo "════════════════════════════════════════"
            cd "$PROJECT_DIR" || { echo "ERROR: No se encontró el directorio"; exit 1; }
            
            git fetch origin || { echo "ERROR: No se pudo descargar de GitHub"; exit 1; }
            git checkout deploy || { echo "ERROR: No se pudo cambiar a rama deploy"; exit 1; }
            git pull origin deploy || { echo "ERROR: No se pudo hacer pull de deploy"; exit 1; }
            
            echo "✓ Código actualizado correctamente"
            echo ""
            echo "════════════════════════════════════════"
            echo "🐳 DETENIENDO CONTENEDORES ANTIGUOS"
            echo "════════════════════════════════════════"
            docker compose down || { echo "ERROR: No se pudo detener contenedores"; exit 1; }
            
            echo "✓ Contenedores detenidos"
            echo ""
            echo "════════════════════════════════════════"
            echo "🔨 CONSTRUYENDO E INICIANDO CONTENEDORES"
            echo "════════════════════════════════════════"
            docker compose up -d --build || { echo "ERROR: No se pudo iniciar contenedores"; exit 1; }
            
            echo "✓ Contenedores iniciados"
            echo ""
            echo "════════════════════════════════════════"
            echo "✅ DESPLIEGUE COMPLETADO EXITOSAMENTE"
            echo "════════════════════════════════════════"
            docker ps
            echo ""
            echo "Estado de docker-compose:"
            docker compose ps
